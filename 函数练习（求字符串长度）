#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int strlenth(char* arr)
{
	int count = 0;
	while(*arr != '\0')
	{
		count++;
		arr++;
	}
	return count;
}
int main()
{
	char arr[] = "abcdefg";
	int len = strlenth(arr);
	printf("len=%d\n", len);
	return 0;
}
